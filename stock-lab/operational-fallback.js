// Operational continuity controller. It never fabricates data or statistical claims.
(function(){
  const POLICY_URL='./operational-fallback-policy.json';let state={mode:'INPUT_ONLY',reason:'尚未檢查',policy:null,readiness:null};
  async function j(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw Error(`${url} ${r.status}`);return r.json()}
  async function factsReady(){try{const c=await window.StockLabSameOrigin?.loadCache?.();if(!c)return false;const legal=c.schema_version===2&&c.source==='licensed-open-data-cache'&&String(c.licence||'').includes('OGDL');const d=c.datasets||{};return legal&&((Array.isArray(d.twse_snapshot)&&d.twse_snapshot.length>0)||(Array.isArray(d.tpex_snapshot)&&d.tpex_snapshot.length>0))}catch{return false}}
  function paint(){const el=document.querySelector('#readinessDetails');if(!el)return;let box=document.querySelector('#operationalMode');if(!box){box=document.createElement('div');box.id='operationalMode';box.className='source-note compact-note';el.insertAdjacentElement('beforebegin',box)}const map={FULL_MODEL:['完整入場模型','正式入場 Gate 已通過'],VERIFIED_RULES:['已驗證規則模式','可做持倉損益、風險條件與下一步；未校準時不顯示勝率／信心'],INPUT_ONLY:['輸入保存模式','市場資料尚未通過驗證；僅保留使用者輸入']};const x=map[state.mode]||map.INPUT_ONLY;box.innerHTML=`<b>目前運行模式：${x[0]}</b><div class=mini>${x[1]}。${state.reason||''}</div>`}
  async function refresh(){let policy=null,readiness=null;try{policy=await j(POLICY_URL)}catch{}try{readiness=await j('./holding-readiness.json')}catch{}if(window.STOCKLAB_RUNTIME?.productionPredictionReady===true){state={mode:'FULL_MODEL',reason:'正式入場模型 Gate 已通過',policy,readiness};paint();return state}if(await factsReady())state={mode:'VERIFIED_RULES',reason:'入場模型仍驗證中；已持有功能不因 OOS 狀態被鎖住',policy,readiness};else state={mode:'INPUT_ONLY',reason:'目前沒有可用的已驗證市場快照，禁止用其他來源或舊值補洞',policy,readiness};paint();return state}
  window.StockLabOperational={refresh,get state(){return state},get mode(){return state.mode}};refresh();
})();