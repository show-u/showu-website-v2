name: HIGH52 prereg confirmatory validation
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/high52-preregistered-confirmatory.js'
      - '.github/workflows/high52-preregistered-confirmatory.yml'
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/high52-preregistered-confirmatory.js
