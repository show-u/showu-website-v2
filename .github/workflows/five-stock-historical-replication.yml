name: Five-stock historical replication
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/five-stock-historical-replication.js'
      - '.github/workflows/five-stock-historical-replication.yml'
  workflow_dispatch:
jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/five-stock-historical-replication.js
