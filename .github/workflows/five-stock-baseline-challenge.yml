name: Five-stock baseline challenge
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/five-stock-baseline-challenge.js'
      - '.github/workflows/five-stock-baseline-challenge.yml'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/five-stock-baseline-challenge.js
