name: Five-stock research pilot
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/five-stock-pilot.js'
      - '.github/workflows/five-stock-pilot.yml'
jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/five-stock-pilot.js
