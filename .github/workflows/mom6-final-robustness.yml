name: MOM6 final robustness
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/mom6-final-robustness.js'
      - '.github/workflows/mom6-final-robustness.yml'
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/mom6-final-robustness.js
