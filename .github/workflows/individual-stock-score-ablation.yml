name: Individual stock score ablation
on:
 push:
  branches: [main]
  paths:
   - 'stock-api/research/individual-stock-score-ablation.js'
   - '.github/workflows/individual-stock-score-ablation.yml'
 workflow_dispatch:
jobs:
 validate:
  runs-on: ubuntu-latest
  timeout-minutes: 60
  steps:
   - uses: actions/checkout@v4
   - uses: actions/setup-node@v4
     with:
      node-version: '20'
   - run: node stock-api/research/individual-stock-score-ablation.js
