name: Right-tail winner walk-forward validation
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/right-tail-winner-validation.js'
      - '.github/workflows/right-tail-winner-validation.yml'
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/right-tail-winner-validation.js
