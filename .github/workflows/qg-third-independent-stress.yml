name: QG third independent stress validation
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/qg-third-independent-stress.js'
      - '.github/workflows/qg-third-independent-stress.yml'
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/qg-third-independent-stress.js
