name: Revenue accel depth validation
on:
 push:
  branches: [main]
  paths: ['stock-api/research/revenue-accel-depth-validation.js','.github/workflows/revenue-accel-depth-validation.yml']
jobs:
 run:
  runs-on: ubuntu-latest
  timeout-minutes: 60
  steps:
   - uses: actions/checkout@v4
   - uses: actions/setup-node@v4
     with: {node-version: '20'}
   - run: node stock-api/research/revenue-accel-depth-validation.js
