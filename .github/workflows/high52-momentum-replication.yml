name: High52 momentum independent replication
on:
  push:
    branches: [main]
    paths:
      - 'stock-api/research/high52-momentum-replication.js'
      - '.github/workflows/high52-momentum-replication.yml'
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node stock-api/research/high52-momentum-replication.js
